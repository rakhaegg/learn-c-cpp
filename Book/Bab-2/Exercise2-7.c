
/*
*Write a function invert(x,p,n) that returns x with the n bits that begin at
*position p inverted (i.e., 1 changed into 0 and vice versa), leaving the others unchanged
*/


#include <stdio.h>

long invert(long x . long p , long n);
int main(){

    return 0;
}
long invert(long x , long p , long n ){



    return x;
}
